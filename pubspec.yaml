name: test_kerja_screen_wizard
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  dotted_border: ^2.1.0
  dropdown_button2: ^1.9.3
  file_picker: ^5.2.3
  flutter:
    sdk: flutter
  flutter_spinkit: ^5.2.1
  http: ^0.13.5
  dropdown_search: ^5.0.6
  photo_view: ^0.15.0
  path: ^1.9.0
  google_mlkit_text_recognition: ^0.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
